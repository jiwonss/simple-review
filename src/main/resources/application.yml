spring:
  datasource:
    url: jdbc:mysql://localhost:3306/SIMPLE_REVIEW?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    username: qwe
    password: qwe123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
